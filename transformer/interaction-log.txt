Create CLAUDE.md in this folder so far with a single rule. I want you to maintain interaction-log.txt, where you'll save all prompts that I give you along the way
Now, download Attention is all your need paper
move it to docs folder and commit
Ok, now read the paper and let's implement a transformer based on this one. Do you need a dataset or do you know where to get it from? I want to start with a small one
Let's do Tiny Shakespeare
Implement the decoder-only transformer plan, using .venv with python-3.14 and uv for venv creation
Read claude.md and continue
Run the training loop and see if it's learning
Sure
Let's train for a bit longer
And why weights_only is False?
Hmm, but you are not training the tokenizer, are you?
Can't you create a checkpoint every epoch or so?
Are you training using fp32 or bf16 precision?
Can you may be write a small script that benchmarks various parts of the training loop
Why are you saying that bf16 support is limited?
Can you try autograd with bf16 and run the benchmark again?
Also, what about torch.compile, will it improve forward or backward
And commit this
Can you tell me how to know if model is overfitting? Or like how to pick size of the model to the number of parameters?
Can you add safetensors to requirements for this project (as well as numpy, to squash the warning) and figure out how checkpoints can be safed/loaded as safetensors
Commit this
Can you run a small experiment to check if training using bf16 would have any negative impact on the accuracy? I.e. let's delete all checkpoint, start from the same random weights (using manual seed) and say train for say 200 steps using fp32 and bf16
But why use autocast rather than just change the model weights to bf16?
Are Attention is All you need paper mentions anything about precision? But indeed let's cast model to bfloat16 and commit the changes
Search the PDF file /Users/malfet/git/malfet/vibeformer/transformer/docs/attention_is_all_you_need.pdf for any mentions of precision, float16, fp16, bf16, bfloat16, half precision, mixed precision, or numerical precision. Use grep on the extracted text or any means available. Report what you find (or if there's nothing).
Can you print performance (in GFlops if you can easily estimate those, or tokens per second) during the training loop
Let's increase it to 100_000
Is there say a TinyPoe dataset (or can you help me create one) that contains poems by Edgar Allan Poe
Is there a more Poe poems?
And let's try something different. How about poems of Konstantin Balmont (in russian)
Can I have multiple checkpoints if I train on different datasets? I.e. is there a checkpoint name prefix (defaults to dataset name)?
Does it always make sense to train the model on the full dataset? I.e. can one train it initially just on words (to learn vocabulary), then on short phrases and finally on the full text?
I want to do experiment a bit with fine tuning, i.e. train model on say Shakespare, Poe and say Chaucer and then quickly tune the model to produce text that is more clesely aligned with the style of just one author. Or may be like Shakespare but in cyrilic, or Balmont but in English
Let's create say a poetry dataset using well known classic poets from 6-8 languages with different character sets: say English, French, German (Goete please), Russian, Spanish, Czech and Bulgarian
Nice, can you rework the tiny_balmont dataset by adding those unicode marks, but also there are lots of weirdly wraped-around stanzas, and form is weird important in poetry. For example Жажда вырваться из плена, вновь изведать гнет оков. should not have contained all those whitespaces and newlines and just one line of the poem, not two
Yes, commit it
Can you now suggest a change to generate that inserts poem start as first token
No need, it's always better to start from the start of the poem
While running the training I wonder if it's possible to save loss curve into .png file or something (using matplotlib) for example
Also, help me understand why it sometimes spikes
Let's plot loss-curve first (may be also save/append them to CSV)
And I wonder if something like mixed precision would be useful for overcoming the bfloat16 numerics. i.e. would keeping weigths in bfloat16, but gradients in float32 will work/help?
Let's apply autocast to the training script
On the loss curve, it's not really usable, may be one should exclude first few steps?
Also, loss seems to plateau at .91 any reason why?
May be instead of fixed number of steps, cut of should be on say maximum difference between start and end of the plot? Alos may be plot loss on a logarithmic scale?
Great, now when I restart the training from the best checkpoint will it overrwirte the values?
Truncate sounds reasonable
Commit one change for new generate and another for training script changes
